# Daphnet 残差模型相对 RAW 的量化诊断与优化报告

生成日期：2026-08-17  
适用范围：当前 `processed_NBM` 严格角色划分、3 折实验，以及已完成的 GRU/Conv-TCN/Attention-NBM、G1/G2/G3 与 RAW 消融结果。

## 1. 执行结论

### 1.1 当前是否已经证明残差模型领先 RAW？

**尚未。**目前没有一个严格同屏障、同预算、同初始化的多种子实验，证明残差模型在 Sensitivity、Precision、Specificity 和 PR-AUC 上同时领先 RAW。

现有结果呈现两种不同现象：

1. 原 GRU-v1 方案 C 在固定阈值工作点上通常提高 Accuracy、Precision、Specificity 和 F1，但 PR-AUC 未稳定领先，Sensitivity 还略有下降；
2. G1/G2/G3 残差表示可以提高 Sensitivity 和 PR-AUC，但通常以 Precision、Specificity 和 Accuracy 下降为代价。

因此，目前不是“残差完全无效”，而是**残差增强了某一部分 FoG 可分性，却没有稳定保留 RAW 中全部判别信息，也没有被后端稳定地转换为理想工作点**。

### 1.2 目前主要是 NBM 问题，还是后端分类器问题？

结论需要按指标层级回答：

| 问题层级 | 主要责任 | 当前判断 |
|---|---|---|
| PR-AUC、AUROC 等阈值无关排序能力 | NBM + 残差构造接口 | **主要瓶颈** |
| Sensitivity/Precision/Specificity 等固定阈值指标 | 特征可分性 + TCN 泛化 + 阈值策略 | **共同作用** |
| 概率值是否可信、阈值是否跨折稳定 | 后端加权损失与校准 | **明显次要瓶颈** |
| 是否只是 epoch 不够 | TCN 训练预算 | **不是主要问题** |

最有力的量化证据是：

- 在相同 TCN 后端下，仅更换 NBM 骨干，五种子 PR-AUC 从 Transformer-NBM 的 `0.5766` 到 GRU-v1 的 `0.7223`，跨度约 **0.1457**；
- 将单一残差 `r` 改为 `[r, |r|, Δr]`，历史严格表示实验的 PR-AUC 提高约 **0.0333**；
- 将 TCN 从 10/2 增加到 50/6 后，G2 的测试 PR-AUC 只提高约 **0.0070**；对 14 个更换了最佳 checkpoint 的 run，总体验证 PR-AUC 平均增加 `0.0201`，但测试 PR-AUC 仅增加 `0.0014`。

由此可见，**对排序能力而言，前端 NBM/残差表示的影响量级显著大于单纯增加后端训练预算**。但是，G2 已表现出较好的排序能力，却仍有较低的 Precision/Specificity，这一部分属于后端工作点与校准问题。

## 2. 证据分级与比较原则

下列结果不能全部混成一个排行榜。报告按证据强度分层：

1. **最高证据**：同目录、同测试屏障、同折、同种子、同 TCN 初始化、仅输入分支不同；
2. **中等证据**：相同角色协议、折与种子，但结果来自不同目录，未建立统一全局屏障或初始化 pair-id；
3. **辅助证据**：旧预算、单种子、不同采样率或不同后端，只用于提出机制假设，不能证明新方案领先。

统计时必须注意：

- 三折的标准差不是随机种子稳定性；
- 高度重叠的窗口不是独立样本，不能把窗口当作独立重复进行显著性检验；
- 测试集只能在模型、阈值和校准全部冻结后运行；
- PR-AUC 不依赖单个分类阈值，概率校准不能提高样本排序本身。

## 3. 严格多种子结果：目前最可靠的现状

### 3.1 五种子 GRU-v1 方案 C 与 RAW：严格配对

统计口径：每个种子宏平均 3 折，再对 5 个种子计算均值。共同后端为当前 TCN。

| 方法 | Accuracy | Sensitivity | Precision | Specificity | F1 | PR-AUC | AUROC |
|---|---:|---:|---:|---:|---:|---:|---:|
| RAW | 0.8853 | **0.8735** | 0.4838 | 0.8867 | 0.6190 | **0.7272** | 0.9530 |
| GRU-v1 方案 C | **0.9018** | 0.8662 | **0.5246** | **0.9060** | **0.6505** | 0.7223 | **0.9558** |
| 残差 − RAW | **+0.0165** | −0.0073 | **+0.0408** | **+0.0193** | **+0.0314** | −0.0049 | +0.0028 |

跨五种子的配对差值为：

| 指标 | 均值 ± 种子 SD | 残差优于 RAW 的种子数 |
|---|---:|---:|
| Accuracy | +0.0165 ± 0.0164 | 3/5 |
| Balanced Accuracy | +0.0060 ± 0.0093 | 3/5 |
| Precision | +0.0408 ± 0.0409 | 4/5 |
| Sensitivity | −0.0073 ± 0.0315 | 1/5 |
| Specificity | +0.0193 ± 0.0212 | 3/5 |
| F1 | +0.0314 ± 0.0280 | 4/5 |
| PR-AUC | −0.0049 ± 0.0206 | 3/5 |
| AUROC | +0.0028 ± 0.0041 | 4/5 |

解释：GRU-v1 不是整体失败。它在当前阈值策略下减少了误报、提高了报警可信度和 F1；但其阈值无关 FoG 排序能力没有稳定超过 RAW，而且平均 Sensitivity 略降。因此它更像是**工作点交换**，而不是全面优势。

跨被试也存在明显异质性：GRU-v1 相对 RAW 的 Accuracy、Precision、Specificity 和 F1 在 8/8 个被试的均值上提高，但 Sensitivity 仅 2/8 提高，PR-AUC 仅 4/8 提高。S01 的 PR-AUC 差值约为 `−0.3185`，说明某些受试者的有效 RAW 信息被残差变换严重削弱，宏平均数会掩盖这一风险。

### 3.2 更换 NBM 骨干：后端相同，结果差异很大

五种子、相同 RAW 基线和相同 TCN 预算下：

| NBM 前端 | Accuracy | Sensitivity | Precision | Specificity | PR-AUC |
|---|---:|---:|---:|---:|---:|
| RAW | 0.8853 | 0.8735 | 0.4838 | 0.8867 | **0.7272** |
| GRU-v1 | **0.9018** | 0.8662 | **0.5246** | **0.9060** | 0.7223 |
| GRU Mask 8–12 | 0.8962 | **0.8775** | 0.5117 | 0.8984 | 0.7246 |
| GRU-v1.5 Decoder96 | 0.9021 | 0.8626 | 0.5260 | 0.9067 | 0.7215 |
| Conv-TCN-v1 | 0.8759 | 0.8545 | 0.4583 | 0.8784 | 0.6744 |
| GRU-v2 | 0.8828 | 0.8674 | 0.4712 | 0.8846 | 0.6782 |
| Transformer-NBM | 0.8443 | 0.7929 | 0.3856 | 0.8503 | 0.5766 |

这组结果说明：

- 更复杂或更强的 NBM 并不自动更好；
- 扩大 GRU decoder 到 96 基本没有收益；
- Conv-TCN、GRU-v2 和 Transformer 前端都明显降低残差可分性；
- GRU Mask 8–12 是目前最接近“PR-AUC 非劣且固定工作点改善”的方案，但其 PR-AUC 仍比 RAW 低 `0.0026`，且种子方向不完全一致。

因此，不建议继续通过盲目堆叠 Attention、Mamba 或扩大 decoder 来优化。现有证据更支持保留简单 GRU-v1，并改进其输入输出接口和防信息丢失机制。

## 4. G1/G2/G3：残差公式比继续加深 NBM 更有价值

定义：

- G1：`clip((e-b)/σ)` 后再次逐窗口逐轴中心化；
- G2：`clip((e-b)/σ)`，不再次中心化；
- G3：`asinh((e-b)/σ)`，不截断、不再次中心化；
- 三组均使用 `[r, |r|, Δr] ∈ R^(27×128)`。

### 4.1 TCN 10 epoch / patience 2

| 组 | Accuracy | Sensitivity | Precision | Specificity | PR-AUC |
|---|---:|---:|---:|---:|---:|
| G1 | 0.8879 ± 0.0112 | **0.9003 ± 0.0183** | 0.4900 ± 0.0280 | 0.8865 ± 0.0146 | 0.7349 ± 0.0096 |
| G2 | **0.8926 ± 0.0074** | 0.8914 ± 0.0115 | **0.4971 ± 0.0187** | **0.8927 ± 0.0095** | **0.7414 ± 0.0130** |
| G3 | 0.8863 ± 0.0077 | 0.8885 ± 0.0156 | 0.4812 ± 0.0158 | 0.8860 ± 0.0103 | 0.7391 ± 0.0077 |

与相同协议、相同三个种子的历史 RAW 目录交叉配对，平均差值为：

| 组 − RAW | ΔAccuracy | ΔSensitivity | ΔPrecision | ΔSpecificity | ΔPR-AUC |
|---|---:|---:|---:|---:|---:|
| G1 | −0.0071 | **+0.0375** | −0.0169 | −0.0123 | **+0.0095** |
| G2 | −0.0024 | +0.0286 | −0.0098 | −0.0060 | **+0.0159** |
| G3 | −0.0087 | +0.0257 | −0.0258 | −0.0127 | +0.0136 |

G1 的 Sensitivity 和 PR-AUC 在 3/3 个种子上都高于该 RAW；G2 的平均 PR-AUC 增益最大，但仅 2/3 个种子为正。由于 RAW 与 G 组不在同一全局测试屏障中，也没有统一初始化 pair-id，这些结果属于**强候选证据，而不是正式领先结论**。

### 4.2 TCN 50 epoch / patience 6

| 组 | Accuracy | Sensitivity | Precision | Specificity | PR-AUC |
|---|---:|---:|---:|---:|---:|
| G1 | 0.8823 ± 0.0051 | **0.9045 ± 0.0126** | 0.4746 ± 0.0078 | 0.8797 ± 0.0071 | 0.7359 ± 0.0050 |
| G2 | 0.8834 ± 0.0032 | 0.9011 ± 0.0093 | 0.4809 ± 0.0129 | 0.8813 ± 0.0045 | **0.7483 ± 0.0135** |
| G3 | **0.8878 ± 0.0056** | 0.8893 ± 0.0113 | **0.4862 ± 0.0098** | **0.8876 ± 0.0075** | 0.7333 ± 0.0087 |

`G2 PR-AUC = 0.7483` 是当前最有希望的结果之一，但该目录没有 RAW 50/6 对照，不能将它直接与 RAW 10/2 或 5/2 的 `0.7255/0.7272` 作正式优越性结论。

## 5. NBM 前端的具体问题

### 5.1 GRU-NBM 更接近“低幅正常基线投影”，不是高保真重构器

在一个已核验的 fold0/seed0 永久测试集诊断中：

| 窗口类型 | 窗口数 | MAE 中位数 | MAE 四分位范围 | RMSE 中位数 | 输入-重构 Pearson r 中位数 |
|---|---:|---:|---:|---:|---:|
| Non-FoG | 2259 | 0.1491 | 0.0375–0.6827 | 0.2368 | 0.0371 |
| FoG | 264 | 0.4526 | 0.2400–0.6094 | 0.7803 | 0.0492 |

FoG/Non-FoG 的 MAE 中位数比为 `3.03`，说明残差确实包含异常信息；但两类 IQR 大量重叠，而且输入与重构相关性都很低。一个 Non-FoG 中位误差代表窗的重构 RMS/输入 RMS 仅为 `0.144`、相关系数约 `0.139`。这表明 GRU-NBM 经常输出接近零的小幅平滑轨迹，而不是忠实恢复个体的正常步态波形。

这会产生两个后果：

1. FoG 的大幅异常会留下明显残差，Sensitivity 可能提高；
2. 正常步态中的峰值、相位、个体幅值差异也被当成残差，造成 Non-FoG 假阳性，并使不同受试者的残差尺度不一致。

因此不能把该模型表述为“已证明把 FoG 重构成配对 Non-FoG”。更准确的表述是：**只在 Non-FoG 上学习一个受限投影，并期望异常偏离该投影；其效果必须通过残差分离指标验证。**

### 5.2 当前损失容易奖励低幅输出

逐窗口中心化后，大量时刻接近零；SmoothL1 对稀疏峰值的相对权重有限。强瓶颈模型因此可能通过输出低幅平均形状获得较低验证损失。历史上直接改为复合损失并没有解决：Gaussian40 方案 C 的 PR-AUC 约为 `0.6998`，对应复合损失实验降至约 `0.6594`。所以不应立即全面切换复合损失。

更合理的顺序是：

1. 先保留已验证最强的 GRU-v1 和 SmoothL1；
2. 优先保留 RAW 信息并优化残差接口；
3. 若再改 NBM 损失，只做小幅、单变量的正常峰值/通道均衡损失，并设置防零塌缩指标。

### 5.3 需要新增的 NBM 诊断指标

NBM checkpoint 仍只由角色 5 的 Non-FoG 验证损失恢复，不能用角色 2/3 继续更新 NBM。但每个冻结 checkpoint 应额外报告：

- 角色 5 每轴 MAE、RMSE、相关系数；
- 每轴 `RMS(X_hat)/RMS(X)`，监控近零输出；
- 正常峰值召回率和一阶差分误差；
- 角色 2/3 上冻结残差的 FoG/Non-FoG 中位数比、效应量与简单残差分数 PR-AUC；
- 不同记录、不同被试的 Non-FoG 残差尺度漂移。

角色 2/3 只能用于完整冻结流水线的开发选择；如果反复比较大量 NBM 架构，需要再做内层开发划分，避免对角色 2/3 过拟合。

## 6. 后端 TCN 的具体问题

### 6.1 增加训练预算没有带来稳定测试收益

对 G1/G2/G3 的 27 个严格配对 TCN run：

- 50/6 在 14/27 个 run 找到了比 10/2 更晚的最佳 checkpoint；
- 只有 5/27 个最佳 epoch 大于 10；
- 所有任务最晚在 epoch 22 结束，没有任何任务接近 epoch 50；
- 对 14 个更换 checkpoint 的 run，验证 PR-AUC 平均提高 `0.0201`，测试 PR-AUC 仅提高 `0.0014`，只有 7/14 的测试 PR-AUC 提高；
- 分组测试 PR-AUC 变化为：G1 `+0.0010`、G2 `+0.0070`、G3 `−0.0058`。

因此，patience=2 确实会错过少量验证集反弹，但**最大 epoch 不足不是主要瓶颈**。50/6 已足够，继续增加到 100 或 200 缺乏证据。

### 6.2 当前表现更像过拟合和验证外推不足

从 10/2 增加到 50/6 后，最终训练 weighted-BCE 均值从 G1/G2/G3 的约 `0.316/0.329/0.332` 降到 `0.203/0.183/0.185`；但最终验证 BCE 反而从约 `0.496/0.529/0.486` 变为 `0.648/0.560/0.567`。训练损失显著下降而验证损失未同步改善，是过拟合证据，而不是分类器容量不足。

验证 PR-AUC 增量与测试 PR-AUC 增量的相关性分别约为 G1 `−0.432`、G2 `0.035`、G3 `−0.100`。这说明当前角色 2/3 的 checkpoint 排序对角色 0/1 外推较弱，尤其不能根据单个验证峰值乐观推断测试提升。

### 6.3 阈值和概率尺度不稳定

50/6 的阈值范围为：

- G1：`0.05–0.74`；
- G2：`0.07–0.90`；
- G3：`0.05–0.88`。

14 个更换 checkpoint 的 run 中，阈值平均绝对移动 `0.349`，最大移动 `0.77`。该现象与 `pos_weight` 加权 BCE、被试域偏移及验证集较小共同相关。它说明概率值不能被解释为跨折统一的真实 FoG 概率。

校准可以改善概率可解释性和阈值可迁移性，但有一个严格边界：**Platt scaling 或其他单调校准不会改变排序，因此不会提高 PR-AUC；若阈值搜索已足够精细，它也不会凭空创造更优的混淆矩阵。**

### 6.4 当前 TCN 与残差特征可能存在归纳偏置不匹配

当前 TCN 约 135,585 个参数，每折约 3,080 个高度重叠的训练窗；卷积主干的理论感受野约 61 点，即 `0.953 s`，随后使用 Global Average Pooling。它在 RAW 上表现良好，说明模型本身并非普遍失效；但对需要比较整段 2 秒残差结构、偏移和相位的任务，约 1 秒感受野加全局平均可能丢失顺序信息。BatchNorm 的运行统计也可能放大跨角色、跨被试域偏移。

这属于需要验证的次级假设，而不是已证明的主因。建议在输入表示确认后，再比较 GroupNorm/LayerNorm、全窗时域聚合或 InceptionTime，避免同时改变前端和后端而失去因果解释。

## 7. 如何让残差方案真正领先 RAW

### 7.1 第一优先级：不要再要求残差单独承担全部信息

目前最可靠的工程方向是**残差 + RAW 混合输入**。理由是：

- 残差对 FoG 异常具有放大作用；
- RAW 包含 NBM 可能擦除的个体幅值、相位、低频姿态和特定轴瞬态；
- 同一 TCN 对 RAW 已有较强表现，保留 RAW 支路可以形成信息安全网。

建议首先比较：

| 臂 | 输入 | 通道数 | 目的 |
|---|---|---:|---|
| R0 | 当前中心化、角色4 Scaler 的 RAW | 9 | 严格基线 |
| R1 | G1：`[r,|r|,Δr]` | 27 | 稳定 Sensitivity/PR 候选 |
| R2 | G2：`[r,|r|,Δr]` | 27 | 最大平均 PR 候选 |
| H-Z | G2 残差 27 通道 + 三个传感器的 RAW 垂直轴 | 30 | 验证用户提出的 Z 轴信息假设 |
| H-ALL | G2 残差 27 通道 + RAW 9 通道 | 36 | 检验完整 RAW 信息上限 |

垂直轴必须由数据字段名确认，不能仅按数组下标假设为 Z 轴。RAW 支路继续使用角色 4 拟合的同一 RobustScaler 和当前逐窗口逐轴中心化；G2 支路不再二次中心化。

为保证公平，不应让 30/36 通道模型仅因参数更多而获益。推荐每个输入支路先经过 `1×1 Conv` 投影到固定宽度，再进入相同 TCN；报告参数量，并尽可能使主干初始化配对。若拼接有效，再升级为双支路晚期融合或可学习门控。

### 7.2 第二优先级：先修正评价工作点，再谈分类器“更强”

建议把主工作点预先定义为以下之一：

1. **匹配 RAW Sensitivity** 后比较 Specificity 和 Precision；
2. 约束 `Sensitivity ≥ 0.90`，在验证集最大化 Specificity；
3. 若目标是报警可信度，则在固定 Recall 下比较 Precision。

这比各模型分别最大化 Balanced Accuracy 后直接对比更科学，因为不同阈值目标会自然产生 Sensitivity/Specificity 交换。

对 weighted-BCE 输出，可在每折角色 2/3 上拟合一个低容量 Platt calibrator，并按记录分组做内部 OOF 预测；然后冻结 calibrator 和阈值，再运行角色 0/1。不要直接把三折验证 logits 池化校准，如果当前测试受试者曾在其他折进入校准集，否则会产生跨折受试者泄漏。

### 7.3 第三优先级：只在严格诊断后修改 TCN

先在相同输入上完成三个探针：

1. 简单统计分数：每轴 MAE/RMS/峰值/差分能量的 PR-AUC；
2. 低容量线性或浅层分类器；
3. 当前 TCN。

判读规则：

- 简单分数和浅层模型都差，TCN 也差：主要是 NBM/残差不可分；
- 简单分数已有较好 PR-AUC，但 TCN 没有兑现：主要是后端训练或聚合问题；
- TCN 显著优于线性探针但仍低于 RAW：残差包含非线性信息，但仍有信息损失；
- H-Z/H-ALL 明显高于残差单支路：证明 NBM 擦除了有用 RAW 信息。

若后端确实受限，再单变量比较：

- BatchNorm → GroupNorm/LayerNorm；
- Global Average Pooling → attention pooling 或统计池化（mean + max + std）；
- 当前 TCN → 参数量匹配的 InceptionTime；
- 感受野覆盖完整 128 点，但保持参数量和训练预算匹配。

不建议此时继续单纯提高 max epoch；现有 50/6 结果已显示验证外推而非训练不足是主要矛盾。

### 7.4 NBM 本身的后续优化顺序

1. 保留 GRU-v1 主干、Z16 和现有无长跳连约束；
2. 以 G1/G2 和 H-Z/H-ALL 先验证信息接口；
3. 保留 40/40/20 增强作为基线；Mask 8–12 可作为独立候选，因为它接近 PR-AUC 非劣；
4. 对每轴重构损失做均衡，防止低幅轴或少数瞬态被总体 SmoothL1 淹没；
5. 仅在防零塌缩指标确认后，小范围测试峰值加权或频域/差分辅助项；
6. 暂不优先扩大 decoder、使用 GRU-v2、Transformer 或更复杂 Attention，因为现有实证均未提高 PR-AUC。

## 8. 最有判别力的下一组正式实验

### 8.1 阶段 A：输入因果消融

一次性运行 `R0/R1/R2/H-Z/H-ALL` 五臂：

- 3 个相同角色折；
- 优先使用 5 个配对种子 `0/52/161/5216/52161`；资源有限时先用 `0/52/161` 筛选，但最终结论必须扩展到 5 种子；
- 所有臂使用 TCN `max_epoch=50, patience=6`；
- 相同 batch size、学习率、pos_weight、阈值搜索规则；
- 相同模型主干初始化 pair-id；
- 同一 `TRAINING_BARRIER` 后统一运行测试；
- NBM、Scaler、`b` 和 `σ` 完全冻结；
- 测试集不参与 epoch、架构、校准或阈值选择。

该实验必须先补齐 RAW 50/6，才能判断 `G2=0.7483` 是否真实领先，而不是预算差异。

### 8.2 阶段 B：前端/后端二因素实验

若阶段 A 仍无法定位，再做参数量匹配的二因素设计：

| 因素 | 水平 |
|---|---|
| 输入 | RAW、G2、H-Z 或 H-ALL |
| 后端 | 当前 TCN、改进后端（优先 InceptionTime 或全窗聚合 TCN） |

量化三个效应：

- 输入效应：`Residual − RAW`；
- 后端效应：`NewBackend − TCN`；
- 交互效应：`(Residual_new − RAW_new) − (Residual_TCN − RAW_TCN)`。

如果更换后端只同时提升 RAW 和 residual，而 `Residual − RAW` 基本不变，主因仍是前端；如果新后端只显著提升 residual，说明当前 TCN 与残差结构不匹配。

## 9. 预注册的“领先 RAW”判定标准

不建议以某一个种子或某一个阈值的均值略高作为成功。建议预先冻结：

1. 主指标：PR-AUC，残差/混合模型相对 RAW 的平均配对增益至少 `+0.010`；
2. 稳定性：至少 4/5 个种子方向为正；
3. 被试一致性：至少 6/8 个被试 PR-AUC 提高，且不能再次出现 S01 级别的大幅塌陷；
4. 临床工作点：在匹配 RAW Sensitivity 或预设 `Sensitivity ≥ 0.90` 时，Specificity 不低于 RAW，并报告 Precision；
5. 不确定性：按被试或记录做 cluster bootstrap，或在 8 个被试上做精确配对置换；不能按重叠窗口计算虚假的窄置信区间；
6. 种子是算法重复，不是独立患者样本；分别报告种子波动与被试波动。

如果研究目标更偏向少漏检，可以把 Sensitivity 设为共同约束，再比较 Specificity/Precision；如果更偏向减少误报，则固定 Recall 后比较 Precision。不存在一个阈值能无条件同时最大化四项指标。

## 10. 最终建议排序

1. **立即补做同屏障 RAW/G1/G2/H-Z/H-ALL，全部用 TCN 50/6。**这是目前信息增益最高的实验。
2. **首选 H-Z 或 H-ALL 混合输入，而不是继续加深 NBM。**它最有机会同时保留 RAW 判别力和残差异常放大能力。
3. **G1 与 G2 都保留。**G1 当前跨种子方向更稳定；G2 平均 PR-AUC 更高且保留偏移信息。
4. **使用匹配 Sensitivity 的工作点评价，并做每折无泄漏的低容量概率校准。**这解决 Precision/Specificity 的解释问题，但不能代替 PR-AUC 提升。
5. **只有在探针确认后端未兑现残差信息后，再改 TCN 的归一化、全窗聚合或骨干。**
6. **不再把提高 max epoch、扩大 decoder 或直接加入复杂 Attention/Transformer 作为第一优先级。**已有量化结果不支持这些方向。

综合判断：**当前“不能稳定领先 RAW”的第一责任是 NBM 输出及残差表示造成的信息损失和跨被试不稳定；后端 TCN 的验证外推、时域聚合和概率阈值问题是第二责任，并主要影响固定阈值指标。**最可能成功的路线不是在两者中二选一，而是用 RAW+残差混合输入消除前端信息损失，再用严格配对训练和匹配 Sensitivity 的阈值策略解决后端工作点问题。

## 11. 主要证据文件

- 五种子 GRU-v1 与 RAW：`outputs/daphnet_tcn_nbm300_C_vs_raw_tcn_ep5pat2_seedset_0_52_161_5216_52161/`
- 三种子 GRU-v1 与 RAW：`outputs/daphnet_gru_nbm300_C_vs_raw_tcn_ep10pat2_seedset_0_52_161/`
- 五种子 Conv-TCN-v1：`outputs/2daphnet_tcn_nbm300_C_vs_raw_tcn_ep5pat2_seedset_0_52_161_5216_52161/`
- 五种子 GRU-v2：`outputs/daphnet_gru_v2_nbm300_C_vs_raw_tcn_ep5pat2_seedset_0_52_161_5216_52161/`
- 三种子 ResTCN-AttnPool-Z16：`outputs/daphnet_restcn_attnpool_z16_nbm300_C_vs_raw_tcn_ep10pat2_seedset_0_52_161/`
- G1/G2/G3 TCN 10/2：`outputs/daphnet_gru_nbm300_residual_G1_G2_G3_tcn_ep10pat2_seedset_0_52_161/`
- G1/G2/G3 TCN 50/6：`outputs/daphnet_gru_nbm300_residual_G1_G2_G3_tcn_ep50pat6_seedset_0_52_161/`
- GRU-NBM Non-FoG 重构诊断：`outputs/figures/gru_nbm_nonfog_reconstruction/gru_nbm_nonfog_reconstruction_metrics.json`
- GRU-NBM FoG 重构诊断：`outputs/figures/gru_nbm_fog_reconstruction/gru_nbm_fog_reconstruction_metrics.json`
- 旧残差表示比较：`outputs/daphnet_processed_NBM_conv_tcn_residual_repr_compare_seed20260807/representation_comparison.csv`

